<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Module category -->
        <record id="module_category_erfasst_connector" model="ir.module.category">
            <field name="name">Erfasst Connector</field>
            <field name="sequence">51</field>
        </record>

        <!-- User group: can import times, view sync log -->
        <record id="group_erfasst_user" model="res.groups">
            <field name="name">Benutzer</field>
            <field name="category_id" ref="module_category_erfasst_connector"/>
            <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
        </record>

        <!-- Manager group: can push projects/planning, configure API, manage mappings -->
        <record id="group_erfasst_manager" model="res.groups">
            <field name="name">Manager</field>
            <field name="category_id" ref="module_category_erfasst_connector"/>
            <field name="implied_ids" eval="[(4, ref('erfasst_connector.group_erfasst_user'))]"/>
            <field name="users" eval="[(4, ref('base.user_root')), (4, ref('base.user_admin'))]"/>
        </record>

    </data>
</odoo>
